# Evil Gary — Python dependencies
# Install with: pip install -r requirements.txt

# Discord
discord.py>=2.3.2

# OpenAI
openai>=1.30.0

# Vector DB & embeddings
chromadb>=0.5.0

# Async HTTP (transitive, pinned for stability)
aiohttp>=3.9.0

# Utilities
python-dotenv>=1.0.0    # for local .env support
